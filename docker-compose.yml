version: '3.8'

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: farroway
      POSTGRES_PASSWORD: farroway_secret
      POSTGRES_DB: farroway_prod
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U farroway"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
      # DATABASE_URL must be supplied via env-var. Falls back to
      # a local-only default for `docker compose up` smoke tests
      # (the db service is not exposed outside the compose
      # network). Production deploys ALWAYS set DATABASE_URL.
      DATABASE_URL: ${DATABASE_URL:-postgresql://farroway:farroway_dev_only@db:5432/farroway_prod?schema=public} # secrets-scanner:ignore
      JWT_SECRET: ${JWT_SECRET:-change-this-to-a-strong-random-secret-in-production}
      JWT_EXPIRES_IN: 24h
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:4000}
    volumes:
      - uploads:/app/server/uploads

volumes:
  pgdata:
  uploads:
