# Farroway Frontend Environment Variables
# Copy to .env for local development, or set at build time for deployment.

# API base URL — only needed for Capacitor native builds or custom deployments.
# Web builds use '/api' by default (proxied by Vite in dev, served by Express in prod).
# VITE_API_URL=https://your-api-server.com/api
