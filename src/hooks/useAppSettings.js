/**
 * useAppSettings — thin re-export for callers that prefer the hook
 * living under /hooks alongside useCropCycles, useUSRecommendations,
 * etc. The actual context lives in src/context/AppSettingsContext.jsx.
 */
export { useAppSettings } from '../context/AppSettingsContext.jsx';
